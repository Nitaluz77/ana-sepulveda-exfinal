import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ana.sepulveda.exfinal',
  appName: 'ana-sepulveda-exfinal',
  webDir: 'www'
};

export default config;
